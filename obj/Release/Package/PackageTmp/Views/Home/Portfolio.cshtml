<br />
<div class="row">
    @*
        ===============
        BATHROOM IMAGES
        ===============
    *@
    <div class="col-md-4">
        <div id="carousel1" class="carousel slide" data-interval="false">

            <!-- Wrapper for slides -->
            <div class="carousel-inner" role="listbox">
                @{
                    if (ViewBag.BathroomImages != null && ViewBag.BathroomImages.Count > 0)
                    {
                        bool FirstActiveImage = true;
                        foreach (var image in ViewBag.BathroomImages)
                        {
                            var base64 = Convert.ToBase64String(image.image);
                            var imgSrc = String.Format("data:image/gif;base64,{0}", base64);

                            string classname = (FirstActiveImage) ? "item active" : "item";
                            <div class="@classname"><img src="@imgSrc" /></div>
                            FirstActiveImage = false;
                        }
                    }
                }
            </div>

            <!-- Left and right controls -->
            <a class="left carousel-control" href="#carousel1" role="button" data-slide="prev">
                <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="right carousel-control" href="#carousel1" role="button" data-slide="next">
                <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>
    </div>

    @*
        ==============
        KITCHEN IMAGES
        ==============
    *@
    <div class="col-md-4">
        <div id="carousel2" class="carousel slide" data-interval="false">

            <!-- Wrapper for slides -->
            <div class="carousel-inner" role="listbox">
                @{
                    if (ViewBag.KitchenImages != null && ViewBag.KitchenImages.Count > 0)
                    {
                        bool FirstActiveImage = true;
                        foreach (var image in ViewBag.KitchenImages)
                        {
                            var base64 = Convert.ToBase64String(image.image);
                            var imgSrc = String.Format("data:image/gif;base64,{0}", base64);

                            string classname = (FirstActiveImage) ? "item active" : "item";
                            <div class="@classname"><img src="@imgSrc" /></div>
                            FirstActiveImage = false;
                        }
                    }
                }
            </div>

            <!-- Left and right controls -->
            <a class="left carousel-control" href="#carousel2" role="button" data-slide="prev">
                <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="right carousel-control" href="#carousel2" role="button" data-slide="next">
                <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>
    </div>

    @*
        ============
        PATIO IMAGES
        ============
    *@
    <div class="col-md-4">
        <div id="carousel3" class="carousel slide" data-interval="false">

            <!-- Wrapper for slides -->
            <div class="carousel-inner" role="listbox">
                @{
                    if (ViewBag.PatioImages != null && ViewBag.PatioImages.Count > 0)
                    {
                        bool FirstActiveImage = true;
                        foreach (var image in ViewBag.PatioImages)
                        {
                            var base64 = Convert.ToBase64String(image.image);
                            var imgSrc = String.Format("data:image/gif;base64,{0}", base64);

                            string classname = (FirstActiveImage) ? "item active" : "item";
                            <div class="@classname"><img src="@imgSrc" /></div>
                            FirstActiveImage = false;
                        }
                    }
                }
            </div>

            <!-- Left and right controls -->
            <a class="left carousel-control" href="#carousel3" role="button" data-slide="prev">
                <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="right carousel-control" href="#carousel3" role="button" data-slide="next">
                <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>
    </div>

</div>
<br />
<div class="row">
    @*
        ===============
        BEDROOM IMAGES
        ===============
    *@
    <div class="col-md-4">
        <div id="carousel4" class="carousel slide" data-interval="false">

            <!-- Wrapper for slides -->

            <div class="carousel-inner" role="listbox">
                @{
                    if (ViewBag.BedRoomImages != null && ViewBag.BedRoomImages.Count > 0)
                    {
                        bool FirstActiveImage = true;
                        foreach (var image in ViewBag.BedRoomImages)
                        {
                            var base64 = Convert.ToBase64String(image.image);
                            var imgSrc = String.Format("data:image/gif;base64,{0}", base64);

                            string classname = (FirstActiveImage) ? "item active" : "item";
                            <div class="@classname"><img src="@imgSrc" /></div>
                            FirstActiveImage = false;
                        }
                    }
                }
            </div>

            <!-- Left and right controls -->
            <a class="left carousel-control" href="#carousel4" role="button" data-slide="prev">
                <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="right carousel-control" href="#carousel4" role="button" data-slide="next">
                <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>
    </DIV>
 
        @*
            ===============
            PAINTING IMAGES
            ===============
        *@
        <div class="col-md-4">
            <div id="carousel5" class="carousel slide" data-interval="false">
                <!-- Wrapper for slides -->
                <div class="carousel-inner" role="listbox">
                    @{
                        if (ViewBag.FlooringImages != null && ViewBag.FlooringImages.Count > 0)
                        {
                            bool FirstActiveImage = true;
                            foreach (var image in ViewBag.FlooringImages)
                            {
                                var base64 = Convert.ToBase64String(image.image);
                                var imgSrc = String.Format("data:image/gif;base64,{0}", base64);

                                string classname = (FirstActiveImage) ? "item active" : "item";
                                <div class="@classname"><img src="@imgSrc" /></div>
                                FirstActiveImage = false;
                            }
                        }
                    }
                </div>

                <!-- Left and right controls -->
                <a class="left carousel-control" href="#carousel5" role="button" data-slide="prev">
                    <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="right carousel-control" href="#carousel5" role="button" data-slide="next">
                    <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
        </div>
    
    <div class="col-md-4">
        @*
            ===============
            FLOORING IMAGES
            ===============
        *@
        <div id="carousel6" class="carousel slide" data-interval="false">

            <!-- Wrapper for slides -->

            <div class="carousel-inner" role="listbox">
                @{
                    if (ViewBag.PaintingImages != null && ViewBag.PaintingImages.Count > 0)
                    {
                        bool FirstActiveImage = true;
                        foreach (var image in ViewBag.PaintingImages)
                        {
                            var base64 = Convert.ToBase64String(image.image);
                            var imgSrc = String.Format("data:image/gif;base64,{0}", base64);

                            string classname = (FirstActiveImage) ? "item active" : "item";
                            <div class="@classname"><img src="@imgSrc" /></div>
                            FirstActiveImage = false;
                        }
                    }
                }
            </div>

            <!-- Left and right controls -->
            <a class="left carousel-control" href="#carousel6" role="button" data-slide="prev">
                <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="right carousel-control" href="#carousel6" role="button" data-slide="next">
                <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>

     </DIV>
    </div>